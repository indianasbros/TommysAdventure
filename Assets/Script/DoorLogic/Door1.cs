using UnityEngine;

public class Door1 : Doors
{ /*
    public bool puzzle1;
    public bool Puzzle1Solved
    {
        get { return puzzle1; }
        set
        {
            puzzle1 = value;
        }
    }
    
    protected override void OpenDoor()
    {

        if (Input.GetKeyDown(KeyCode.Space) && canOpen && puzzle1)
        {
            if (!isOpen)
            {
                targetAngle = (initialAngle - 80f + 360f) % 360f; // abre 80 grados
                isOpen = true;
            }
            else
            {
                targetAngle = initialAngle; // cierra de vuelta
                isOpen = false;
            }
        }
    }
    */
}